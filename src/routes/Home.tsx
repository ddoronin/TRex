import * as React from 'react';
import AppPane from "../widgets/AppPane";

const Home = () => (
    <article>
        <section>
            <AppPane/>
        </section>
    </article>
);

export default Home;
