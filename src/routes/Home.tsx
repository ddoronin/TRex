import * as React from 'react';
import AppPane from "../widgets/AppPaneWidget";

const Home = () => (
    <article>
        <section>
            <AppPane/>
        </section>
    </article>
);

export default Home;
